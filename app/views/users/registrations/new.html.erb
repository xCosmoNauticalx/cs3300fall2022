<!DOCTYPE html>
<html lang="en">
<head>
  <title>Project Portfolio</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js"></script>
<style>
  .banner {
    padding: 25px;
    background-color: #AA7DCE;
    margin-top: 0;
  }
  a {
    color: #3B429F
  }
  .project-button {
    background-color: #A8577E;
    color: white;
  }
  .text-margin {
    margin-top: 1%;
  }
</style>

<body style="background-color: #F5D7E3;">

  <div class="banner text-center">
    <h1>PORTFOLIO</h1>
  </div>
  <br>

  <div class="text-center">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), class:"form-horizontal") do |f| %>
      <%= render "users/shared/error_messages", resource: resource %>

      <div class="field form-group">
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email" %>
      </div>

      <div class="field form-group">
        <%= f.password_field :password, autocomplete: "new-password", placeholder: "Password", class: "text-margin" %>
      </div>

      <div class="field form-group">
        <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "Confirm password", class: "text-margin" %>
      </div>
      <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
      <br>
        <div class="actions">
          <%= f.submit "Sign up", class: "btn project-button" %>
        </div>
    <% end %>
    <br>
    <%= render "users/shared/links" %>
    </div>
</body>
</html>
